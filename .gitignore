# PlatformIO
.pio/
.pioenvs/
.piolibdeps/

# Local firmware .bin backups -- binary build artifacts, not source;
# regenerable from git history + a rebuild. Kept locally for quick
# recovery flashing without needing this machine's Claude session again.
firmware_backups/

# Secrets (real file is gitignored, .example version is committed)
include/secrets.h

# Editor/OS cruft
.vscode/
*.swp
.DS_Store
Thumbs.db
